# RELEASE-CARD-TONES v0.26

**목적**: 릴리즈 카드 문구 2가지 톤 버전 (딱딱/친근)

## 핵심 결론 (1줄)

다음(v0.26)은 v0.25 릴리즈 카드 문구를 딱딱한 톤 / 친근한 톤 두 가지로 분리해서 바로 골라 쓰게 만드는 패킷입니다.

---

## 릴리즈 카드 톤 버전

아래 2세트 중 하나를 골라 그대로 릴리즈 카드(이미지 1장)에 붙여넣으면 됩니다.

### SET A: 딱딱(업무용)

**헤드라인(택1)**
• "Ops 명령 복사 기능 추가 및 /map 빌드 안정화"
• "/ops Actions 도입, /map Suspense 패턴 적용"
• "운영 워크플로우 개선: ops 복사 + map 빌드 규칙 준수"

**서브헤드(1줄)**
• "/ops에서 issue/proof/decision 생성 명령을 즉시 복사할 수 있으며, /map은 Client 분리+Suspense로 빌드 오류를 제거했습니다."

**핵심 포인트(3)**
• "/ops: Actions + 3 Copy 버튼 + clipboard fallback + Copied! 피드백"
• "/map: Server page + MapClient 분리, useSearchParams는 Client 전용"
• "검증: pnpm build Exit 0 / dev에서 /ops·/map 정상"

**푸터(1줄)**
• "Release tag: v0.21.0"

**CTA(택1)**
• "변경사항 확인"
• "검증 결과 보기"
• "업데이트 적용"

---

### SET B: 친근(SNS용)

**헤드라인(택1)**
• "클릭 한 번으로 ops 명령 복사 끝!"
• "/map 빌드 에러, 깔끔하게 해결했어요"
• "ops는 더 빠르게, map은 더 안정적으로"

**서브헤드(1줄)**
• "이제 /ops에서 버튼만 누르면 명령이 복사되고, /map은 규칙대로 정리해서 빌드가 안정적으로 돌아가요."

**핵심 포인트(3)**
• "버튼 3개로 issue/proof/decision 명령 복사"
• "복사 안 될 때도 자동으로 대체 방식으로 복사"
• "빌드/실행 확인 완료: /ops OK, /map OK"

**푸터(1줄)**
• "v0.21.0 업데이트"

**CTA(택1)**
• "지금 써보기"
• "업데이트 확인"
• "바로 적용!"

---

## 예상 효과 & ROI

- **상황(내부 보고 vs SNS)에 맞는 톤을 바로 선택해서** 커뮤니케이션 시간을 줄입니다.

---

## 필요시 협업 제안

- **클로이**: v0.26 톤 버전 제공 (딱딱/친근)
- **자비스(나)**: 원하시면 다음(v0.27)은 **노션/블로그에 붙여넣는 1페이지 릴리즈 노트(짧고 읽기 쉽게)**로 만들어드리겠습니다.

---

## 사용 예시

### SET A: 딱딱(업무용) - 내부 보고서, 이메일, 공식 문서

```
[상단]
운영 워크플로우 개선: ops 복사 + map 빌드 규칙 준수

[중단]
/ops에서 issue/proof/decision 생성 명령을 즉시 복사할 수 있으며,
/map은 Client 분리+Suspense로 빌드 오류를 제거했습니다.

[하단]
✓ /ops: Actions + 3 Copy 버튼 + clipboard fallback + Copied! 피드백
✓ /map: Server page + MapClient 분리, useSearchParams는 Client 전용
✓ 검증: pnpm build Exit 0 / dev에서 /ops·/map 정상

[우하단]
Release tag: v0.21.0
```

### SET B: 친근(SNS용) - 블로그, SNS, 팀 채팅

```
[상단]
ops는 더 빠르게, map은 더 안정적으로

[중단]
이제 /ops에서 버튼만 누르면 명령이 복사되고,
/map은 규칙대로 정리해서 빌드가 안정적으로 돌아가요.

[하단]
✓ 버튼 3개로 issue/proof/decision 명령 복사
✓ 복사 안 될 때도 자동으로 대체 방식으로 복사
✓ 빌드/실행 확인 완료: /ops OK, /map OK

[우하단]
v0.21.0 업데이트
```

---

## 상태
- **생성일**: 2025-12-22
- **목적**: 릴리즈 카드 톤 버전 제공 (딱딱/친근)
- **이전 버전**: v0.25 (RELEASE-CARD-COPY)
- **다음 버전**: v0.27 (노션/블로그 1페이지 릴리즈 노트 예정)
